---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  require_chef_omnibus: '12.2'
  client_rb:
    audit_mode: :audit_only

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[audit_test::default]
      - recipe[audit_test::breakit]
    attributes:

